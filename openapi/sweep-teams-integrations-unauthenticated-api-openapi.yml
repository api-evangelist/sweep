openapi: 3.2.0
info:
  title: Sweep Teams Integrations (Unauthenticated) API
  description: This is a publicly available Sweep API.
  version: '0.1'
  contact: {}
servers:
- url: https://api.sweep.io
  description: Sweep production API — the host this document was harvested from (GET /api-json); /health responds here
tags:
- name: Teams Integrations (Unauthenticated)
paths:
  /teams-integrations/message:
    post:
      operationId: TeamsIntegrationUnauthenticatedController_handleMessage
      parameters: []
      responses:
        '201':
          description: ''
      tags:
      - Teams Integrations (Unauthenticated)
components:
  securitySchemes:
    bearer:
      scheme: bearer
      bearerFormat: JWT
      type: http
