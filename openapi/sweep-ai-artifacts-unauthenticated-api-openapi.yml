openapi: 3.2.0
info:
  title: Sweep AI Artifacts (unauthenticated) API
  description: This is a publicly available Sweep API.
  version: '0.1'
  contact: {}
servers:
- url: https://api.sweep.io
  description: Sweep production API — the host this document was harvested from (GET /api-json); /health responds here
tags:
- name: AI Artifacts (unauthenticated)
paths:
  /public/artifacts/{id}:
    get:
      operationId: AiArtifactsUnauthenticatedController_getPublicArtifact
      parameters:
      - name: id
        required: true
        in: path
        schema:
          type: string
      - name: token
        required: true
        in: query
        schema:
          type: string
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: object
      tags:
      - AI Artifacts (unauthenticated)
components:
  securitySchemes:
    bearer:
      scheme: bearer
      bearerFormat: JWT
      type: http
