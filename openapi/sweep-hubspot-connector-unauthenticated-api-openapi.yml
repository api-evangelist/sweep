openapi: 3.2.0
info:
  title: Sweep hubspot connector (unauthenticated) API
  description: This is a publicly available Sweep API.
  version: '0.1'
  contact: {}
servers:
- url: https://api.sweep.io
  description: Sweep production API — the host this document was harvested from (GET /api-json); /health responds here
tags:
- name: hubspot connector (unauthenticated)
paths:
  /hubspot-connector/redirect-to-oauth:
    get:
      operationId: HubSpotConnectorController_redirectToHubspotOAuthPath
      parameters: []
      responses:
        '200':
          description: ''
      tags:
      - hubspot connector (unauthenticated)
components:
  securitySchemes:
    bearer:
      scheme: bearer
      bearerFormat: JWT
      type: http
