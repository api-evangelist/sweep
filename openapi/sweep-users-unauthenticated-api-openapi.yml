openapi: 3.2.0
info:
  title: Sweep Users (unauthenticated) API
  description: This is a publicly available Sweep API.
  version: '0.1'
  contact: {}
servers:
- url: https://api.sweep.io
  description: Sweep production API — the host this document was harvested from (GET /api-json); /health responds here
tags:
- name: Users (unauthenticated)
paths:
  /users/{userSubId}/resend-verification-email:
    post:
      operationId: UsersUnauthenticatedController_resendVerificationEmail
      parameters:
      - name: userSubId
        required: true
        in: path
        schema:
          type: string
      responses:
        '201':
          description: ''
      tags:
      - Users (unauthenticated)
components:
  securitySchemes:
    bearer:
      scheme: bearer
      bearerFormat: JWT
      type: http
