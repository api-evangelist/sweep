openapi: 3.2.0
info:
  title: Sweep Dashboard API
  description: This is a publicly available Sweep API.
  version: '0.1'
  contact: {}
servers:
- url: https://api.sweep.io
  description: Sweep production API — the host this document was harvested from (GET /api-json); /health responds here
tags:
- name: Dashboard
paths:
  /dashboard:
    get:
      operationId: DashboardController_getAccountStatus
      parameters: []
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: object
      security:
      - bearer: []
      tags:
      - Dashboard
  /dashboard/accountDetails:
    put:
      operationId: DashboardController_updateUserAndAccountName
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserAndAccountDetails'
      responses:
        '200':
          description: ''
      security:
      - bearer: []
      tags:
      - Dashboard
components:
  schemas:
    UpdateUserAndAccountDetails:
      type: object
      properties: {}
  securitySchemes:
    bearer:
      scheme: bearer
      bearerFormat: JWT
      type: http
